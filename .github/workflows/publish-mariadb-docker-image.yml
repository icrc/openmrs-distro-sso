name: Publish MariaDB docker image with openmrs dump

on:
  push:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Build the Docker image
        run: docker build ./database --file ./database/Dockerfile --tag mariadb-10:2.5.12
